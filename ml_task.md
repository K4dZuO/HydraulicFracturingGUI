### Формальная постановка задачи регрессии параметров квадратичной трансформации

**1. Исходные данные и объекты:**

Пусть имеется выборка, состоящая из `N` объектов (скважин), для каждого из которых известны:

*   **Априорные параметры скважины и пласта (статичные):**
    `S_i = [h_i, W_i, L_i, ...] ∈ R^M` — вектор известных до проведения ГДИС параметров (мощность пласта, ширина трещины, полудлина и т.д.).
*   **Расчётная гидродинамическая кривая (динамическая):**
    `Y_calc_i = [y_{calc_i}^{(1)}, y_{calc_i}^{(2)}, ..., y_{calc_i}^{(T)}] ∈ R^T` — вектор значений (например, давления `P` или логарифмической производной `dP`), рассчитанный по модели на основе `S_i` для фиксированного вектора условий/времени `X = [x^{(1)}, x^{(2)}, ..., x^{(T)}]`.
*   **Эталонная (наблюдаемая) кривая ГДИС:**
    `Y_true_i = [y_{true_i}^{(1)}, y_{true_i}^{(2)}, ..., y_{true_i}^{(T)}] ∈ R^T` — вектор фактически замеренных значений в ходе ГДИС для тех же условий `X`.

**2. Локальная (точечная) модель трансформации:**

Для каждой i-й скважины существует **идеальное квадратичное преобразование**, параметризованное вектором `θ_i = [a_i, b_i, c_i]^T ∈ R^3`, которое наилучшим образом (в смысле MSE) отображает расчётную кривую в эталонную:
`Y_true_i ≈ f(X, Y_calc_i; θ_i) = a_i * Y_calc_i + b_i * 1 + c_i * (Y_calc_i)^2`,
где умножение и возведение в квадрат поэлементные, `1` — вектор из единиц длины `T`.

Параметры `θ_i` находятся решением задачи МНК для каждой скважины в отдельности:
`θ_i = argmin_{θ} || Y_true_i - f(X, Y_calc_i; θ) ||_2^2`.

**3. Глобальная (обобщающая) задача мета-регрессии:**

Цель — построить модель `G`, которая по **совокупности априорных признаков скважины и дескрипторов её расчётной кривой** предсказывает параметры оптимальной трансформации `θ`.

*   **Признаковый вектор:** `Φ_i = concat[S_i, Ψ(Y_calc_i)]`.
    Здесь `Ψ: R^T -> R^K` — оператор извлечения информативных дескрипторов из кривой `Y_calc_i` (статистики: среднее, дисперсия, эксцесс; экстремумы; производные; площадь под кривой; энергия и т.д.).
*   **Целевой вектор:** `θ_i` (рассчитанный на шаге 2).
*   **Задача:** Найти функцию `G: R^{(M+K)} -> R^3`, минимизирующую среднюю ошибку предсказания параметров трансформации на выборке:
`G^* = argmin_{G} Σ_{i=1}^{N} L(θ_i, G(Φ_i))`,
где `L` — функция потерь (например, среднеквадратичная ошибка по всем трём компонентам).

**4. Использование `P` и `Q` (дебита):**

*   **Включение в признаки:** Если для скважины известны векторы замеров давления `P_i` и дебита `Q_i` (не только итоговые `Y_calc_i/Y_true_i`), их статистики также можно включить в `Ψ`. Это обогатит признаки информацией о **режиме проведения ГДИС**.
*   **Уточнение модели трансформации:** Преобразование можно сделать зависимым от условий: `Y_true_i ≈ a_i * Y_calc_i + b_i * 1 + c_i * (Y_calc_i)^2 + d_i * Q_i`. Это усложнит модель, но может повысить адекватность, если влияние дебита систематично.

**Итоговая цель:** Получить модель `G^*`, которая для **новой скважины** с известными `S_new` и `Y_calc_new` предскажет параметры `θ_pred = G^*(Φ_new)`. Применение преобразования `f(X, Y_calc_new; θ_pred)` даст **скорректированную расчётную кривую**, максимально приближенную к ожидаемому результату реального ГДИС, что позволит провести предварительную интерпретацию или валидацию модели без осуществления дорогостоящих замеров.